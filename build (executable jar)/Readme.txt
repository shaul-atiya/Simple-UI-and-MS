Please see current jar details:

You will find 'demo-0.0.1-SNAPSHOT.jar' which is the artifact - executable jar of spring boot project.
the jar contains single microsrvice that holds RESTful web-services for all the CRUD operations, the microservice connected to MongoDB Atlas.
You will find the compiled UI in this jar as well.

To run the jar (start the server) you will need to download the jar  and run the next under the jar folder
java -jar demo-0.0.1-SNAPSHOT.jar
or run the attached batch file

To access the web-application (once local server is runing) go to:
http://localhost:8080/

Thank you.
